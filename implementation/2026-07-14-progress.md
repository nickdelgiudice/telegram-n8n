# Progress — 2026-07-14

## Summary

Scaffolded the first n8n workflow connecting Telegram to n8n, using the newly-registered `n8n-workflow-builder-dev` MCP server (built via the `n8n-workflow-builder` skill). Pushed the workflow to the local dev n8n instance and committed the on-disk copy to this repo.

## What was done

1. **Verified new MCP tools** after a session restart — `n8n-workflow-builder-dev` (workflow authoring against dev n8n) and `lucid` (Lucidchart) were newly available.
2. **Fixed a stale Docker mount**: the `n8n-workflow-builder-dev`/`-prod` containers' bind mount of `/home/nicol/dev` was stale and couldn't see this project directory (Docker Desktop/WSL2 file-share needed a refresh). Restarted both containers to fix it; this required a further Claude Code session restart to reconnect the MCP stdio connection.
3. **Scaffolded workflow `Telegram Bot`**:
   - **Telegram Trigger** (`n8n-nodes-base.telegramTrigger`) — fires on `message` updates.
   - **Chat ID Gate** (`n8n-nodes-base.if`) — compares `{{ $json.message.chat.id }}` against a placeholder `YOUR_TELEGRAM_CHAT_ID`.
   - **Downstream Placeholder** (`n8n-nodes-base.noOp`) — stub wired to the IF node's true branch, for future automation.
4. **Pushed to live dev n8n** (`http://localhost:5678`, live workflow id `LGDFRDKFfvyTkhPN`) via `create_live_workflow`. This confirmed the workflow is actually valid — the local MCP validator (`validate_workflow`) currently flags every node as `unknown_node_type` because its static schema catalog is empty (`get_n8n_version_info` reports `supportedNodesCount: 0`); this is a tool-side quirk, not a real problem with the workflow.
5. **Committed and pushed**: `workflow_data/Telegram Bot.json` and an updated `CLAUDE.md` (commit `b8e1047`, pushed to `origin/main`).

## Still to do (manual, in the n8n UI)

- Add a **Telegram API** credential to the Telegram Trigger node (BotFather HTTP API token) — the MCP tools can't set credentials.
- Replace the `YOUR_TELEGRAM_CHAT_ID` placeholder in the Chat ID Gate node with a real chat ID (send the bot a message once the trigger is active/listening, read `message.chat.id` off the resulting execution).
- Ensure `WEBHOOK_URL` is set on the n8n instance to a public HTTPS URL (ngrok or similar for local dev) so Telegram can deliver webhook updates.
- Replace the `Downstream Placeholder` NoOp node with real automation logic.

## Known issues / notes

- `n8n-workflow-builder-dev`'s local `validate_workflow` / `add_node` schema catalog is currently empty (`hasLocalNodeData: false`, `supportedNodesCount: 0`, `capabilities: ["basic_nodes"]`). It reports `unknown_node_type` for essentially all node types, including core ones like `if` and `noOp`. Cross-checked against the live n8n instance, which accepts these node types without issue — treat local validation output as unreliable until the catalog is repopulated.
- Restarting either `n8n-workflow-builder-*` container drops the Claude Code MCP stdio connection to it and requires a Claude Code session restart (`/exit` then `claude -c`) to reconnect.

## Later same day: chat ID fix, Pocket integration, dedup fix, migration

6. **Fixed the Chat ID Gate placeholder** — the string `YOUR_TELEGRAM_CHAT_ID` tripped the IF node's strict number-type validation. Set it to a numeric placeholder, then to the real chat ID (`1449057581`) once the user found it via an n8n execution. Also synced in the credential/webhook-ID fields the user set by hand in the n8n UI (commits `32613fc`, `5d91f70`).
7. **Built a reusable notifier**: new workflow `Send Telegram Message` (live id `1IrnXdUpfHhhT5R8`, activated) — `Execute Workflow Trigger` (passthrough) → `Telegram sendMessage`, callable from any workflow with `{ message }`.
8. **Wired it into the pre-existing `Pocket - Inbound` workflow** (live id `2UI2FJmTqSxx3MpH`, not otherwise part of this repo — a reference copy was added at `workflow_data/Pocket - Inbound.json`, later removed once superseded by the migration below) so a Telegram message goes out when a Pocket recording's transcription/summary is ready (commit `eb45959`).
9. **Fixed a duplicate-message bug**: Pocket fires 6 distinct lifecycle webhooks per recording (`recording.created`, `transcription.completed`, `speakers.labeled`, `mind_map.completed`, `summary.updated`, `summary.completed`) — all legitimate, not retries. Added a `Pocket - Is Final Event?` gate (`event === 'summary.completed'`) so exactly one Telegram message goes out per recording, with a 100-char summary excerpt in the body. Also caught and fixed a latent bug where the Telegram branch fed into the same `respondToWebhook` node (`Pocket - Ack`) as the (currently detached) Assistant handoff branch — two incoming branches into one `respondToWebhook` node risks a "response already sent" error (commit `059833d`). User confirmed only one message now arrives per recording.
10. **Migrated this repo's content into `n8n_prod_flows/telegram_bot/`** (a separate, longer-lived repo at `/home/nicol/dev/n8n_prod_flows` that organizes production n8n flows by topic folder) — copied `workflow_data/*.json` and docs, folded `CLAUDE.md` into that repo's top-level `README.md` as a new `## telegram_bot` section, matching its existing convention. Content only, not git history — this `telegram-n8n` repo's `.git` was left untouched. `n8n_prod_flows` commit `3125658` (not yet pushed — that repo had unrelated uncommitted work in progress, so only the migration-related files were staged).

**This `telegram-n8n` repo is now effectively superseded by `n8n_prod_flows/telegram_bot/`.** It hasn't been archived or deleted (a deliberate choice — deletion is destructive and wasn't explicitly requested); user was given options for retiring it (archive vs. delete, locally and on GitHub) but hadn't decided as of end of session.
